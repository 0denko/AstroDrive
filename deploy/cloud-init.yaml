#cloud-config
package_update: true
packages:
  - curl

runcmd:
  - [ bash, -lc, "curl -fsSL https://raw.githubusercontent.com/REPLACE_ME/AstroDrive/main/deploy/install.sh | bash -s -- https://github.com/REPLACE_ME/AstroDrive.git" ]
